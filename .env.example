# Server Configuration
NODE_ENV=production
PORT=3000
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Auto-loop Configuration (Reduced limits for Render)
AUTO_LOOP=false
LOOP_INTERVAL=1800000
MAX_SESSIONS=5
DEFAULT_TARGET_URL=https://example.com

# Proxy Configuration (optional)
DEFAULT_PROXIES=ip1:port:username:password,ip2:port:username:password

# Puppeteer Configuration (Render automatically sets this)
PUPPETEER_EXECUTABLE_PATH=
